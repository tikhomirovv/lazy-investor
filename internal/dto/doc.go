// Package dto holds data types used as the contract between exchange API (broker) and application.
// Adapters convert broker-specific responses into these types; app and domain use only dto (and ports).
package dto
